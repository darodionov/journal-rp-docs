# Дельта DEBT-B.md — 2026-09-14 (итоги P2–P4 декомпозиции B2)

Применяется Claude Code через `/debt-apply` (`apply_delta.py`); перевыпуск
файла целиком запрещён протоколом. Закрывать нечего: строка Р-4 закрыта
2026-09-10. Все строки — дописать в конец раздела «Долги».

## Дописать

- [ ] 2026-09-14 #B2 приёмка P2 по тестам открыта (тесты запаркованы решением владельца 2026-09-08): нет unit-тестов шести списковых методов порта (`listArticlesByRubrika`, `listRPArticlesByRubrika`, `listRPArticlesByNomer`, `listNomera`, `listZhitiyaByMonth`, `listZhitiyaByIstochnik`) и `nomerComparator` на подвыборке; нет негативной типовой проверки `@ts-expect-error` (`listZhitiyaByMonth(13)` не компилируется); после них — smoke на статьях журнала. Сегодня проверено только дымовым скриптом `scripts/smoke/not_commit/smoke-b2-p2-p3.ts` вне коммита
- [ ] 2026-09-14 #B2 приёмка P3 по тестам открыта: `routes.test.ts` импортирует старые хелперы поимённо и новые (`razdelPath`, `zhurnalRubrikaPath`, `zhurnalNomerPath`, `zhitiyaMonthPath`, `zhitiyaIstochnikPath`, `statiRubrikaPath`, `istoriyaZhurnalaPath`, `pagePath`) не проверяет; нет снапшота пятнадцати адресов подрубрик и источников готовыми строками; нет тест-сторожа резерва `page`/`nomer` по таблицам рубрик и месяцев; нет тестов `paginate`/`pageCount`/`pagesAfterFirst`/`pageFromSegment`/`monthFromSlug` на границах. Границы проверены дымовым скриптом 2026-09-11…12
- [ ] 2026-09-14 #B2 папка страницы пагинации номеров журнала лежит не там: билд-лог `next build` 2026-09-14 показывает маршрут `/journal/nomer/page/[n]` вместо `/journal/nomer/[nomer]/page/[n]`; перенести `src/app/journal/nomer/page/[n]/page.tsx` в `src/app/journal/nomer/[nomer]/page/[n]/page.tsx` и убедиться по билд-логу. Дефект не проявился, потому что при `PAGE_SIZE = 20` ни у одного номера нет второй страницы (максимум 4 статьи у № 26 и № 27) — прямое свидетельство, что снапшот адресов из строки выше нужен
- [ ] 2026-09-14 #B2 приёмка P4 «ноль ошибок гидрации» проверялась глазами в консоли на нескольких страницах; системного прохода по всем 126 адресам билд-лога нет — сделать в P6 вместе с вёрсткой по макету, когда появится клиентская разметка
- [ ] 2026-09-14 #B2 18 статей журнала из базы (легаси-тип 5, без `zhurnalRubrika` и `magazineNum` по ADR-0008-m п. 4) и история журнала (`legacyId 0`) после решения владельца 2026-09-10 (хаб `/journal` без общей ленты, эррата к ADR-0008-m п. 7) изнутри сайта недостижимы: в подрубриках их нет, в перечне номеров не участвуют; достижимы только прямым адресом, редиректами и sitemap. Три пути — назначить подрубрику руками (данные, а не код), дверь «Статьи о журнале» на хабе с собственным адресом, оставить как есть; решение владельца — вопрос №4 декомпозиции B2 ред. №4
- [ ] 2026-09-14 #B2 три хаба (`/zhitiya`, `/journal`, `/stati`) повторяют по форме загрузчик дверей «перечень значений → вызов порта → `Door`» (`loadRubriki`, `loadNomera`, `loadKarliki` и обход месяцев/источников); вынести общий после P6, когда макет покажет, одинаково ли устроены хабы
- [ ] 2026-09-14 #B2 на страницах пагинации `/stati/page/<n>` нет дверей карликов — они только на первой странице `/stati` (двери рисует файл маршрута, а страницы пагинации идут через фабрику `createFeedRoutes`); решить по макету в P6, где место дверям
- [ ] 2026-09-14 #B2 файлы маршрутов житий, журнала и `/stati` попарно повторяют объявление `const routes = createFeedRoutes({ entries, resolve })` в `page.tsx` и `page/[n]/page.tsx`; терпимо при шести парах, выносить в общий модуль, если пар станет заметно больше
- [ ] 2026-09-14 #B2 подписи разделов для дровера (P8) и русские заголовки плоских разделов пока живут строками в файлах маршрутов (`rubrikaFeed('pritcha', 'Притчи')` в двух файлах на раздел); при P8 перенести в `labels.ts` одной таблицей под `satisfies`
- [ ] 2026-09-14 #процесс `scripts/smoke/not_commit/` — одноразовые дымовые скрипты вне коммита; добавить путь в `.gitignore`, чтобы папка не попала в индекс git случайно

## Не сюда (арка A — в `DEBT.md`, дом записи один)

- Билд-лог 2026-09-14: Turbopack предупреждает о трассировке всего проекта из-за файловых операций в `src/components/mdx/figure.tsx` (импорт-трейс через `articles/[slug]/page.tsx`); ограничить путь статически или пометить `turbopackIgnore`.
- В `src/app/articles/[slug]/page.tsx` строка `shelfGridRow(article.content);` в начале компонента — мёртвый вызов с выброшенным результатом; комментарий «проверь, как называется поле» у `datePublished` — след отладки.
- Описание сайта в корневом `layout.tsx` — заглушка `Generated by create next app`.
- В билд-логе есть маршруты `/article/smisl` и динамический `/article/smisl/[paramId]` (единственное число `article`) — судя по имени, эксперимент; решить, живёт ли он в продакшен-сборке.
