# Передача шага B2 в новый чат — 2026-09-07

## Состояние

Шаг B2 (индекс метаданных и разделы лент), арка B, режим `#учебник`,
обратный порядок: декомпозицию пишет наставник, код пишет владелец.

Сделано: декомпозиция B2 ред. №3; ADR-0008-m ред. 2026-09-07 (единый
документ, статьи журнала — пп. 4–6, 12, 14); `schema.ts` ред. 2026-09-07;
P1 — код индекса и тесты. P5 закрыт, кроме п. 6 (показ даты).

Следующее дело: P2 — шесть методов порта по сигнатурам из декомпозиции.

## Файлы пакета и куда их класть

| Файл | Куда |
|---|---|
| `b2-decomposition.md` | `journal-docs/ark-b/` (заменить) |
| `0008-m-adr-edinoe-prostranstvo-statei-i-razdely-lent.md` | `journal-docs/adr/` (заменить) |
| `b2-handoff.md` | `journal-docs/ark-b/` |
| `schema.ts` | `src/lib/content/` (заменить) |
| `filesystem-content-source.ts` | `src/lib/content/` — под ИМЕНЕМ существующего модуля фабрики |
| `article-index.test.ts` | рядом с модулем фабрики; синтаксис Vitest — при Jest заменить `vi` на `jest` |

После замены схемы: `tsc` покажет все места с `'vypusk'` — убрать
(карта разделов, если уже есть; тесты схемы: вариант выпуска → вариант
`russkiy_palomnik` с координатами). После коммита — пересинхронизировать
проект, иначе новый чат увидит старые версии.

## Приложить файлами к первому сообщению нового чата

- `src/lib/content/content-source.ts` — порт
- `src/lib/routes.ts`
- `src/lib/content/index.ts`

При старте P6: картинки макета ленты (1600 и мобильный) и код макета из
Claude Design.

## Промпт для чата B1б (до прогона)

```
Три правки S8 по ADR-0008-m ред. 2026-09-07 (п. 4, 6, 14):
1. Для source: "zhurnal" писать rubrika: russkiy_palomnik. Варианта
   vypusk в схеме больше нет; zhurnalRubrika, magazineNum, magazineYear —
   опциональные поля рубрики russkiy_palomnik.
2. magazineNum — СТРОКА как на обложке: "15", для папок 14/15/16 —
   "21-22". Схема: z.string().regex(/^\d+(-\d+)?$/). Второй номер пары
   больше не уходит в отчёт — он в поле. Строку DEBT-B Р-4 закрыть.
3. Сторож в S8: при legacyId ≥ 1 обязательна zhurnalRubrika; legacyId
   в 0..39; 255 у папки 35 — оверрайд в пустое; конфликт папки 32 —
   карантин и оверрайд владельца.
```

## Строки для DEBT-B.md

- [x] закрыть строку Р-4 от 2026-09-04 (сдвоенность номера не выражена)
  — выражена строкой `magazineNum` (ADR-0008-m п. 6).

## Решения владельца этого чата

- 2026-09-02: индекс — вычисление при сборке.
- 2026-09-04: слаги подрубрик заморожены; май — `may`; источники — из
  enum механически; `imeniny` в карте авансом.
- 2026-09-07: одна рубрика `russkiy_palomnik`; номер — строка; адрес
  номера — `/journal/nomer/<номер>`; 18 статей типа 5 — статьи из
  журнала; история журнала — обычная статья, `legacyId 0`.

## Открытые вопросы владельцу

1. P5.6 — показ даты у мигрированных на четырёх поверхностях (до P6).
2. P8 — вход в четыре малых раздела; рекомендация: дровер + футер.
3. P6 — есть ли макет хаба журнала.
4. P4 — хаб `/journal/` совмещать с первой страницей общей ленты или
   разнести (по макету).

## Квиз, накопленный в этом чате (по команде `#квиз`)

1. Чем мемоизация промиса отличается от мемоизации результата, и какую
   гонку закрывает первая?
2. Где живёт `React.cache` и почему он не подходит для индекса сборки?
3. Почему сортировка индекса обязана быть тотальной, и что даёт
   вторичный ключ по слагу?
4. Что делает `satisfies Record<Rubrika, Razdel>` и чем это отличается от
   аннотации типа?
5. Почему адрес раздела должен принадлежать данным, а не коду?
6. Почему слова `page` и `nomer` резервируются на рубричном уровне?
7. Почему `Exclude<Rubrika, 'imeniny'>` лучше ручного перечня — и в каком
   случае хуже?
8. Почему `z.int().min(1).max(12)` выводится в `number`, а объединение
   литералов — нет?
9. Чем подрубрика отличается от фасета (источник против месяца)?
10. Почему пагинация вынесена в чистые функции, а не в аргумент методов
    порта?
11. Почему кэш индекса живёт в замыкании фабрики, а не на уровне модуля?
