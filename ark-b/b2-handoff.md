# Передача шага B2 в новый чат — 2026-09-14

## Состояние

Шаг B2 (индекс метаданных и разделы лент), арка B, режим `#учебник`,
обратный порядок: декомпозицию пишет наставник, код пишет владелец.

Сделано с 2026-09-07: P2 (шесть списковых методов порта), P3 (хелперы
адресов в `src/lib/routes/`, карта разделов, модуль пагинации), P4
(дерево маршрутов шести разделов, черновые компоненты ленты, три хаба).
Гейт Г2 закрыт 2026-09-10. Декомпозиция — ред. №4; ADR-0008-m — с
эрратой 2026-09-14 к п. 7. Тесты P2–P4 запаркованы решением владельца
2026-09-08 и записаны в DEBT-B. Индекс — 60 статей.

Следующее дело: P6 — вёрстка лент по макету. До неё — решение P5.6
(показ даты у мигрированных на четырёх поверхностях) и ответ, есть ли
макет хаба журнала. Параллельно можно вести P7 (главная на индекс) и P8
(шапка и дровер).

## Файлы пакета и куда их класть

| Файл | Куда | Как |
|---|---|---|
| `b2-decomposition.md` (ред. №4) | `journal-docs/ark-b/` | заменить; проверено: 17 заголовков ред. №3 сохранены, решения 1–7 дословно, P1–P9 и P5.1–P5.6 на месте |
| `0008-m-adr-edinoe-prostranstvo-statei-i-razdely-lent.md` | `journal-docs/adr/` | заменить; проверено: исходный текст — байт в байт префикс, дописано 45 строк эрраты |
| `debt-b-delta-2026-09-14.md` | не в репозиторий | Claude Code применяет через `/debt-apply`; 10 строк, формат проверен, дублей нет; раздел «Не сюда» — кандидаты в `DEBT.md` арки A |
| `b2-handoff.md` (этот) | `journal-docs/ark-b/` | заменить |

После коммита — пересинхронизировать проект, иначе новый чат увидит
старые версии. Код в индекс знаний не попадает (проверено 2026-09-07:
поиск не находит даже токены из приложенных `.ts`), поэтому файлы кода
прикладываются вложениями.

## Приложить файлами к первому сообщению нового чата (P6)

Обязательно:

- `b2-handoff.md` — этот файл, читать первым.
- Макет ленты рубрики из Claude Design: картинки 1600 и мобильный, код
  макета. Если есть макет хаба журнала и хаба житий — их тоже.
- `src/components/feed/feed-page.tsx`, `article-card.tsx`,
  `door-list.tsx`; `src/components/paginator/pagination.tsx` — черновые
  компоненты, которые P6 переводит на макет.
- `src/lib/content/labels.ts`, `src/lib/routes/feeds.ts`,
  `src/lib/routes/feed-route.tsx` — данные и фабрика, от которых зависят
  компоненты.
- `src/lib/content/schema.ts` — форма `ArticleMeta` для карточки (дата,
  обложка, `legacyId`, `magazineYear`).
- `src/lib/content/pagination.ts` — ради `PAGE_SIZE`, который уточняется
  по сетке макета.

По необходимости: `src/lib/routes/routes.ts`, `src/app/journal/page.tsx`,
`src/app/zhitiya/page.tsx`, `src/app/stati/page.tsx` — если P6 трогает
хабы; `src/app/articles/[slug]/page.tsx` — при реализации P5.6 на
странице статьи.

## Для Claude Code — что и в каком виде передать

Сеанс один на рабочее дерево (правило DEBT-B 2026-09-04). Порядок:

1. **Переместить папку.** `git mv "src/app/journal/nomer/page" "src/app/journal/nomer/[nomer]/page"`,
   затем `npx next build` и убедиться, что в билд-логе стоит
   `/journal/nomer/[nomer]/page/[n]`, а `/journal/nomer/page/[n]` исчез.
   Основание — строка дельты DEBT-B и P4 декомпозиции ред. №4.
2. **Ревью P2–P4 по чек-листу** (не переписывать, а сверить):
   `npx tsc --noEmit` и `npx eslint src` чистые; `npx vitest run` —
   восемь настоящих файлов тестов зелёные (удвоение из
   `.next/standalone` — открытая строка DEBT-B 2026-09-04, если не
   закрыта, закрыть `include: ['src/**/*.test.ts', 'scripts/**/*.test.ts']`
   в `vitest.config.ts`); `getSortedIndex` без `async`; порт
   `ContentSource` содержит `getArticle`, `listArticles`,
   `getArticleSlugs`, `getSortedIndex` и шесть методов P2 с сигнатурами
   из декомпозиции; `index.ts` реэкспортирует `ZhurnalNomer`,
   `ListableRubrika`, `Rubrika`, `Month`, `ZhurnalRubrika`,
   `ZhitieIstochnik`; вход `src/lib/routes/index.ts` экспортирует только
   `./routes` (не `feeds`, не `feed-route`); стадии конвейера в
   `scripts/migration/` импортируют `articlePath` по рабочему пути после
   переезда `routes.ts`; во всех `[…]`-маршрутах стоит
   `dynamicParams = false`; в разметке лент и хабов внутренние ссылки —
   `next/link`; в `src/app/` нет папок разделов внутри `articles/`.
3. **Применить дельту DEBT-B** через `/debt-apply` из
   `debt-b-delta-2026-09-14.md`; проверка `--check` после применения:
   записей 25, открытых 20, дефектов не найдено. Раздел «Не сюда» —
   четыре кандидата в `DEBT.md` арки A, вносить отдельным заходом по
   решению владельца.
4. **Положить документы** из таблицы выше по своим путям.
5. **Тесты из долга — по отдельному заданию владельца**, не в этом
   заходе: unit-тесты P2 на подвыборке, контракт-тест слеша на новых
   хелперах, снапшот 15 адресов, сторож резерва `page`/`nomer`, границы
   `paginate`/`pageFromSegment`/`monthFromSlug`, негативная типовая
   проверка. Дымовой скрипт `scripts/smoke/not_commit/…` — образец
   ожиданий; в коммит не идёт, путь — в `.gitignore`.
6. **Коммит — только по явному подтверждению владельца.** Состав:
   `src/lib/content/` (порт, адаптер, схема, подписи, пагинация, `index.ts`),
   `src/lib/routes/` (все пять файлов), `src/components/feed/`,
   `src/components/paginator/`, `src/app/{pritchi,dobrotolyubie,nastavleniya,zhitiya,journal,stati}/`,
   `journal-docs/ark-b/b2-decomposition.md`, `journal-docs/ark-b/b2-handoff.md`,
   `journal-docs/adr/0008-m-*.md`, `journal-docs/DEBT-B.md`,
   `.gitignore`. Сообщение коммита предложить владельцу, не сочинять
   молча.
7. После коммита — пересинхронизировать проект в Claude.

## Решения владельца этого чата

- 2026-09-08: тесты P2 (и далее P3, P4) паркуются; имя типа перечня
  номеров — `ZhurnalNomer` в порту; методы второго уровня возвращают
  суженные типы; правило P5.2 применяется буквально.
- 2026-09-10: Г2 закрыт; тип `Razdel` — литералы адресов (вариант А);
  карта `Record<Rubrika, Razdel>` отвечает «раздел», адреса лент —
  хелперы (вариант А); `pageCount(0) = 1` (вариант А); хаб `/journal`
  без общей ленты — ни на хабе, ни по отдельному адресу.
- 2026-09-11: подписи источников житий: «Жития по свт. Димитрию
  Ростовскому», «Жития по свт. Николаю Сербскому», «Избранные жития»;
  `zemnaya_zhizn` — подпись за владельцем (в `labels.ts` стоит
  заглушка `...` до правки).
- 2026-09-12: папка `src/lib/routes/` с входом `index.ts`; `feed-route.tsx`
  живёт там же (рекомендация наставника была `src/components/feed/`,
  владелец выбрал `routes/`).
- 2026-09-14: внутренние ссылки — `next/link`; `STATI_RUBRIKI` и
  `StatiRubrika` остаются в `routes.ts`; подпись двенадцатой подрубрики
  на сайте — «Новый Россійскій Мартирологій» (с прописных; таблица ADR
  п. 5 — «Новый россійскій мартирологій»).

## Открытые вопросы владельцу

1. P5.6 — показ даты у мигрированных на четырёх поверхностях (до P6).
2. P8 — вход в четыре малых раздела; рекомендация: дровер + футер.
3. P6 — есть ли макет хаба журнала.
4. 18 статей журнала без координат и история недостижимы изнутри сайта —
   три пути (см. эррату к ADR-0008-m).
5. P6 — визуальная группировка номеров по годам на хабе журнала.
6. Подпись `zemnaya_zhizn` в `labels.ts`.
7. Строка про `include` для vitest — закрыта ли (не отвечено 2026-09-14).

## Квиз, накопленный к этому моменту (по команде `#квиз`, по одному)

Из чата до 2026-09-07:

1. Чем мемоизация промиса отличается от мемоизации результата, и какую
   гонку закрывает первая?
2. Где живёт `React.cache` и почему он не подходит для индекса сборки?
3. Почему сортировка индекса обязана быть тотальной, и что даёт
   вторичный ключ по слагу?
4. Что делает `satisfies Record<Rubrika, Razdel>` и чем это отличается от
   аннотации типа?
5. Почему адрес раздела должен принадлежать данным, а не коду?
6. Почему слова `page` и `nomer` резервируются на рубричном уровне?
7. Почему `Exclude<Rubrika, 'imeniny'>` лучше ручного перечня — и в каком
   случае хуже?
8. Почему `z.int().min(1).max(12)` выводится в `number`, а объединение
   литералов — нет?
9. Чем подрубрика отличается от фасета (источник против месяца)?
10. Почему пагинация вынесена в чистые функции, а не в аргумент методов
    порта?
11. Почему кэш индекса живёт в замыкании фабрики, а не на уровне модуля?

Из чата 2026-09-07…14:

12. Почему `Exclude<Rubrika, 'vypusk'>` не даёт ошибки компиляции после
    удаления варианта из объединения? (дистрибутивный условный тип)
13. Почему аргумент номера — `string`, а аргумент рубрики — литеральный
    союз? (закрытое множество против открытого)
14. Почему условие `keep` в `selectFromIndex` не сужает тип, а проверка
    `if (nomer === undefined) continue` в цикле сужает?
15. Чем предикат типа `article is T` отличается от `boolean`; когда
    TypeScript 5.5 выводит его сам, а когда нет?
16. Почему функция с `async` никогда не возвращает тот же объект промиса,
    и какой тест это поймал?
17. Почему `.ts`-файл не понимает JSX — какой синтаксис языка с ним
    конфликтует?
18. Чем компонент отличается от функции, возвращающей JSX
    (`<Pagination />` против `Pagination(...)`), и когда прямой вызов
    уместен?
19. Почему после `notFound()` компилятор сужает тип, и при каком условии
    это работает для собственных функций?
20. Почему `Map`, а не объект, для группировки по строковым ключам,
    похожим на числа?
21. Зачем в `MONTH_SLUG` и `as const`, и `satisfies` — что делает каждый?
22. Почему `pageFromSegment` отвергает `02`, `1e1` и `1` — какой принцип
    адресации за этим стоит?
23. Почему функции наборов `first`/`paginated` — стрелки-свойства, а не
    методы объекта? (`unbound-method`, динамический `this`)
24. Почему `await` над обычным значением позволяет типу `MaybeAsync`
    обойтись без ветвления?
25. Почему `dynamicParams = false` и внутренний `notFound()` сторожат
    разное?
26. Почему обратная таблица месяцев строится из прямой программно, а не
    вторым списком?
27. Почему `Feed.load` — функция, а не готовый массив?
28. Почему хаб `/journal` без ленты, а `/pritchi` — лента на базовом
    адресе: какой принцип решает?
29. Почему `const rubrika = 'dobrotolyubie'` проходит как
    `ListableRubrika`, а `let` — нет?
30. Что сторожит `['biografiya','akafist','molitva'] as const satisfies
    readonly Rubrika[]` вместе с `(typeof …)[number]`, а что не сторожит?
31. Как компилятор выводит `P` в `createFeedRoutes` без явного параметра
    типа, и почему `{ ...params, n }` получает тип `P & { n: string }`?

## Промпт для нового чата

```
Шаг B2, арка B, #учебник, обратный порядок: декомпозицию пишет
наставник, код пишу я. Продолжение чата от 2026-09-14.
В knowledge (после коммита и пересинхронизации): b2-handoff.md — читай
первым; b2-decomposition.md ред. №4; adr/0008-m с эрратой 2026-09-14;
DEBT-B.md с дельтой 2026-09-14.
Приложены: b2-handoff.md; макет ленты (картинки 1600 и мобильный, код
из Claude Design); feed-page.tsx, article-card.tsx, door-list.tsx,
pagination.tsx; labels.ts, feeds.ts, feed-route.tsx; schema.ts;
pagination.ts.
P2, P3, P4 сделаны (тесты в долге), Г2 закрыт, папка пагинации номеров
перенесена. Следующее дело: P6 — вёрстка лент по макету. Начни с
вопроса P5.6 (показ даты у мигрированных) и ответа, есть ли макет хаба
журнала; потом разверни цель P6 и поставь первую задачу.
Квиз из файла передачи — только по команде #квиз.
```
