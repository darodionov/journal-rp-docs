# DEBT-B.md — техдолг арки B

> **Протокол.** Одна строка = одна запись. Формат: `- [ ] ГГГГ-ММ-ДД #тег текст`
> Тег — шаг плана (`#B1б`, `#B2`, `#B4`) либо `#процесс`.
> Сделано → меняем `[ ]` на `[x]`, строку не удаляем (история решений).
> Новое дописываем **в конец** раздела.
>
> Файл правится дельтой через `.claude/skills/debt-apply/apply_delta.py`;
> перевыпуск целиком запрещён.
>
> Долги арок A и A′, дизайна, тулинга и документации живут в `DEBT.md`.
> Здесь — конвейер миграции, легаси-данные, редиректы, кодировки.

## Долги
- [x] 2026-09-04 #B1б s6-manifest.json (files[].articles: number[]) и s9-manifest.json (id: number) ключуются числом — при двух источниках записи (articlex, 7) и (zhurnal, 7) склеятся; перевести на пару до J4/J7 → 2026-09-04 переведены на пару (source, legacyId) двумя полями: `s6-manifest.json` несёт `articles: {source, id}[]`, `s9-manifest.json` — `source` рядом с `id`; проверено прогоном S6/S9 и чтением артефактов
- [x] 2026-09-04 #B1б report.ts slugById остался числовым по решению задания — пересмотреть вместе с манифестами → 2026-09-04 `slugByKey` ключуется парой через `corpusKey`; проверено прогоном S11 на корпусе из двух источников — классы 12–13 не путают статью 24 с выпуском 24
- [ ] 2026-09-04 #процесс два сеанса Claude Code одновременно писали в одно рабочее дерево (fdf1d056 в bg-режиме и рабочий); обнаружено случайно, по ошибке компилятора о константе, которой ещё не существовало; ущерба нет — правились скрипты, не данные, сверка велась от HEAD. Правило: один сеанс на рабочее дерево, второй — только чтение или отдельная ветка
- [ ] 2026-09-04 #B1б опечатывание var/corpus.jsonl держалось в том числе правами (владелец dr, 644, конвейеру запись закрыта); на J4 доступ открыт решением владельца, физическая защита заменена сторожами стадии (атомарная запись, побайтная сверка 1338 старых строк, чистый git status на старте) — при следующем пересмотре проверить, что все три живы
- [ ] 2026-09-04 #B1б `!.ogl` не читался ни одной страницей легаси: грепом по всем PHP зеркала вхождений `.ogl` ноль, `jurnal.php` в зеркале не существует — это редакционный артефакт мёртвого движка, а не часть страницы; на J6 решить, попадает ли дореформенный заголовок из него в тело
- [ ] 2026-09-04 #B1б 155 ссылок `jurnal.php` остаются без редиректов (решение Р-5): адресов этих в зеркале нет и правила `.htaccess` для них нет; пересмотреть, если найдутся внешние ссылки на них
- [ ] 2026-09-04 #B1б оглавления 26 выпусков не перенесены (Р-5): главы там — части одного `index.htm`, якорей под них нет; переносится только оглавление выпуска 30, где главы — настоящие файлы
- [x] 2026-09-04 #B1б сдвоенный номер выпуска («№21-22-2000» у 14, 15, 16) в схеме не выражен: в `magazineNum` идёт первый номер пары, второй живёт строкой отчёта J2 — вопрос вёрстки ленты, решается в B2 или не решается вовсе (Р-4) → 2026-09-10 выражен схемой: `magazineNum` — строка печатной обложки (`z.string().regex(/^\d+(-\d+)?$/)`, ADR-0008-m п. 6 ред. 2026-09-07), номер собирается из разбора `!.ogl`, а при его отсутствии из числа базы строкой. Проверено полным прогоном J1→J4 и S2→S11: у папок 14, 15, 16 во фронтматтере `magazineNum: 21-22`, класс отчёта «второй номер пары» заменён на «Номер выпуска: откуда взят» (39 строк с происхождением); два прогона подряд дали совпавшие md5
- [x] 2026-09-04 #B1б четыре выпуска (3, 7, 10, 16) в карантине S5: пять картинок без alt и без соседней подписи `div.picbutton` — ждут строк в `config/alt-texts.ts`, после чего MDX станет 40 вместо 36; кандидаты подписей и контекст — в отчёте J6–J10 → 2026-09-05 владелец дописал четыре подписи в `config/alt-texts.ts` 2026-09-05; проверено полным прогоном: карантин S5 пуст, MDX стало 56 (16 articlex + 40 журнальных), учёт 56 + 3 = 59
- [x] 2026-09-04 #B1б рукотворная карта не перегнана по ADR-0008-m п. 7: 39 строк `article=<N>` и строка `/journal` должны уйти, `history.php` — получить цель `/articles/istoriya-zhurnala`; `data/redirects-manual.tsv` принадлежит владельцу (права 644, владелец dr), правка за ним, готовые строки — в отчёте → 2026-09-05 владелец перегнал карту 2026-09-05: 39 строк `article=<N>` и строка `/journal` убраны, `history.php` получил цель; `EXPECTED_MANUAL_REDIRECTS` приведён к 29, проверено прогоном S10 — рукотворных редиректов 4
- [ ] 2026-09-04 #B1б `npm run build` в сеансе не прогнан: `src/assets/logo-*.svg` лежат с правами 700 у владельца, Turbopack падает на чтении; вместо сборки паритет проверен компиляцией всех 52 MDX стадией S9 и сверкой картинок, якорей и фронтматтера отдельным проходом → 2026-09-05 сборку прогнал владелец (`.next/build` 4 сентября, `.next/standalone` 5 сентября); ограничение прав осталось: сеанс по-прежнему не читает `src/assets/logo-*.svg`
- [ ] 2026-09-04 #B1б вложенные блоки внутри блоков не разбираются: `<p>` внутри цитаты у выпусков 16, 19, 27 и `<blockquote>` внутри абзаца у выпуска 9 схлопываются в строчный текст (счётчик «ещё не реализовано» S5) — содержимое цело, структура беднее
- [ ] 2026-09-04 #B1б папку истории журнала (N=0) стадия J1 не обходит, поэтому её неиспользуемые файлы (2 из 7) не попадают в класс непереносимых; при следующем заходе решить, читать ли папку 0 наравне с выпусками
- [ ] 2026-09-04 #процесс у vitest нет конфига, а умолчание исключает только `node_modules` и `.git`: прогон собирал тесты и из `.next`, поэтому счёт «10 файлов, 64 теста» до сеанса включал копии от сборки 1 сентября, а после переписи `.next` стал честным — 8 файлов, 65 тестов при тех же файлах на диске; задать явный `include` (`src/**` и `scripts/**`), чтобы счёт не зависел от кеша сборки → 2026-09-05 причина подтверждена прямо: сборка кладёт в `.next/standalone/` копию всего исходного дерева вместе с тестами, `find .next -name '*.test.*'` даёт восемь путей; счёт после сборки удваивается — 16 файлов и 130 тестов при восьми настоящих
- [ ] 2026-09-05 #B1б тело выпуска 7 задвоено в самом легаси: `index.htm` — 57 932 символа, это два побайтно одинаковых блока по 28 966; из 40 папок случай единственный (проверено). Конвейер переносит тело дословно, поэтому статья выйдет напечатанной дважды; решение (поимённая половинка в `config/zhurnal.ts` со сторожем равенства половин) ждёт владельца — вопрос В6 отчёта J6–J10
